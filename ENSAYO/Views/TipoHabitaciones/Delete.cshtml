@* @model ENSAYO.Models.TipoHabitacione

@{
    ViewData["Title"] = "Delete";
}

<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>TipoHabitacione</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.NombreTipoHabitacion)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.NombreTipoHabitacion)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.NumeroPersonas)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.NumeroPersonas)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Estado)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Estado)
        </dd>
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="IdTipoHabitacion" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="Index">Back to List</a>
    </form>
</div>
 *@
 @model ENSAYO.Models.TipoHabitacione

@{
    ViewData["Title"] = "Eliminar Tipo de Habitación";
}

<div class="container my-4">
    <h1 class="text-center mb-4 text-danger">Eliminar Tipo de Habitación</h1>

    <!-- Mensaje de confirmación -->
    <div class="alert alert-warning text-center" role="alert">
        <h4>¿Estás seguro que deseas eliminar este tipo de habitación?</h4>
        <p>Esta acción no se puede deshacer.</p>
    </div>

    <!-- Detalles del tipo de habitación -->
    <div class="card shadow-sm p-4 mb-4" style="border-radius: 8px;">
        <h4 class="card-title mb-3">Detalles del Tipo de Habitación</h4>
        <dl class="row">
            <dt class="col-sm-4 font-weight-bold">Nombre del Tipo</dt>
            <dd class="col-sm-8">@Html.DisplayFor(model => model.NombreTipoHabitacion)</dd>

            <dt class="col-sm-4 font-weight-bold">Número de Personas</dt>
            <dd class="col-sm-8">@Html.DisplayFor(model => model.NumeroPersonas)</dd>

            <dt class="col-sm-4 font-weight-bold">Estado</dt>
            <dd class="col-sm-8">
                <span class="badge" style="background-color: @(Model.Estado ? "#28a745" : "#dc3545"); color: white;">
                    @(Model.Estado ? "Activo" : "Inactivo")
                </span>
            </dd>
        </dl>
    </div>

    <!-- Botones de acción -->
    <div class="text-center">
        <form asp-action="Delete" method="post" class="d-inline">
            <input type="hidden" asp-for="IdTipoHabitacion" />
            <button type="submit" class="btn btn-danger mx-2">
                <i class="fas fa-trash-alt"></i> Eliminar
            </button>
        </form>
        <a asp-action="Index" class="btn btn-secondary mx-2">
            <i class="fas fa-arrow-left"></i> Volver a la lista
        </a>
    </div>
</div>



<style>
    .btn {
        transition: background-color 0.3s ease, color 0.3s ease;
    }

    .btn-danger:hover {
        background-color: #bd2130;
        color: white;
    }

    .btn-secondary:hover {
        background-color: #5a6268;
        color: white;
    }

    .card {
        background-color: #f9f9f9;
        border: 1px solid #ddd;
    }

    .font-weight-bold {
        font-weight: bold;
    }

    .badge {
        font-size: 1rem;
        padding: 0.5rem 1rem;
        border-radius: 0.25rem;
    }

    .alert-warning {
        font-weight: bold;
        background-color: #fff3cd;
        border: 1px solid #ffeeba;
    }
</style>
